import './utils/jquery-extensions';
import './utils/easing';

export { isFirefox, isChrome, isWindows } from '../common/utils/detect';
export {
  C,
  toggleConsole,
  nullFn,
  L_getValue,
  L_setValue,
  L_removeValue,
  parseHTML,
  unwrapTag,
  getTextNodesIn,
  toRE,
  toReStr,
  wildcardToRegExpStr,
  getUrlHost,
  sleep,
  DOMContentLoaded,
  Request,
  $x,
} from './utils';
